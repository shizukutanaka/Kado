# Plan.md — Kado "AI-First Geometry Engine"

> 作成日: 2026-06-11 ｜ 改訂: 2026-06-13（ソクラテス問答v4＝セキュリティ境界の吟味 反映）｜ 状態: **取締役会承認待ち**（G1）
> 旧Plan（Forge3D v4.0案）を破棄。ゼロベース再定義（C6）。Forge3D v3.0.0はアーカイブ資産として凍結。
> 本改訂の根拠は `docs/socratic-review.md` に記録（v2＝問1〜11は計画の吟味、v3＝問12〜14は実装の誠実さ、v4＝問15〜17はセキュリティ境界）。
> v3 で固定した不変条件: ①正本＝スクリプトはMCP `Session` 状態として実行時にも成立（問12） ②全プリミティブは表面テストで検証（Cone を厳密式に修正・問13） ③サンプリング境界は `Sdf::aabb` で形状から導出しサイレント・クリッピングを防止（問14）。
> v4 で固定した不変条件: ④MCP書き込みは `sandbox_write_path` でCWD配下に限定しパストラバーサルを拒否（問15・リスクT） ⑤DSL/パーサにリソース上限を強制し病的入力でのクラッシュを防止（問16・リスクE） ⑥決定的出力をend-to-endテストで固定（問17）。

---

## プロダクト定義（C11・1から再定義）

このプロダクトは、AIエージェントが3D形状を生成・検証・製造用出力するために呼び出すローカル幾何モデリングエンジンである。**外部送信ゼロ・決定的出力・単一自己完結バイナリ配布・全機能ヘッドレス動作**を設計上の制約とする。

### 5つの問い
| # | 問い | 答え |
|---|------|------|
| 1 | 誰が使うか | AIエージェント（MCPクライアント）。人間は副ユーザー（CLI + 出力HTMLビューア閲覧。**作図入力はスクリプトテキスト編集に限る／可視化は読み取り専用**） |
| 2 | 何のために | 自然言語意図→形状コード→自律検証→製造可能ファイル、の無人完走 |
| 3 | どこで動くか | ローカル単一バイナリ。CLI / MCPサーバー（stdio）の2形態。**GUIアプリは持たない** |
| 4 | 何を持たないか | GUIアプリ・クラウド・アカウント・外部ML API・CAM/CAE・アセンブリ拘束ソルバ |
| 5 | 外せない制約 | ①外部送信ゼロ ②単一自己完結バイナリ（=実行時外部依存ゼロ。静的リンクは許容、問4） ③決定的出力（範囲は§7で限定） ④ブーリアン演算が**場の代数として**失敗しないこと |

### 旧定義との差分（ソクラテス問答v1の結論）
| 崩した前提 | 問い | 結論 |
|-----------|------|------|
| 「CADはGUIアプリ」 | AIにGUIは必要か | 不要。AIはscreenshotツールで見る。人間には自己完結HTMLビューアを**出力**すれば、GUI実装という最大の保守負債を丸ごと削除できる（C3） |
| 「v3.0メッシュBSP CSGを継承」 | エージェント失敗ループの最大原因は何か | ブーリアン数値破綻。修正ではなく**破綻し得ないカーネル**を選ぶのが正しい問題設定 |
| 「主ユーザーは人間、AIは追加機能」 | 最高峰の基準は誰の体験か | AIの無人完走率。人間向けUXは出力物（HTML/PNG/STL）の品質に還元される |
| 「プロダクト名はForge3D」 | 新定義に旧名は適合するか | 不適合。Kado（角）。**取締役決定済み** |

---

## 1. 目的

AIエージェントにとって世界最高の幾何エンジンを作る。基準は3つ: (a) ブーリアン・オフセット・ブレンドが**場の代数として**定義上失敗しない堅牢性（メッシュ抽出の健全性は別途保証、問1/問11） (b) エージェントが1コマンドで導入できる単一バイナリ (c) 生成→検査→スクリーンショット→自己修正のループを最短化するツール群。調査結論（視覚フィードバックで成功率77.8→90.5%、code-as-truth、検査/snapshot/debugのツール化、MCP事実標準3点セット）を初期設計に織り込む。

**正本（source of truth）の定義（問2で確定）**: 唯一の正本は**スクリプト（DSLソース）**である。特徴履歴・シーングラフはその構文木の決定的な射影（派生ビュー）に過ぎない。MCPの `create_feature / edit_feature / delete_feature` は**構文木操作のシンタックスシュガー**として定義され、最終的に正本スクリプトを書き換える。`get_scene` の「差分」はスクリプト編集前後の射影差分を指す。

## 2. カーネル3案比較（C6必須）

| 軸 | 案A: メッシュCSG継続（v3.0路線） | 案B: 自前B-rep | **案C: SDF/陰関数 + 特徴履歴（採用案）** |
|----|------|------|------|
| 堅牢性（演算） | BSP数値破綻あり（v3既知） | 高いが実装難度極大 | **ブーリアン=min/max。場の代数として原理的に失敗しない** |
| 堅牢性（抽出メッシュ） | 破綻の主因 | 厳密 | **manifold dual contouringで非多様体を回避。naive DCは不可（問11）** |
| フィレット/シェル | 近似困難 | 本格対応 | smooth blend / offsetで自然に表現 |
| 水密性 | 修復処理依存 | 保証可 | 適切なDCで構築時から保証（抽出メッシュ常に閉） |
| 工数 | S（継続） | XL×数年・失敗リスク大 | XL（1製品分・実現可能） |
| 保守性 | Python配布痛・GIL | 巨大コード | 単一バイナリ・コア小 |
| 寸法精度 | 頂点座標そのもの | 厳密 | **プリミティブ寸法は解析的厳密。だが出力メッシュは公差付き近似（問1）。公差はユーザー制御パラメータ** |
| 性能 | 中 | 高 | 高（SIMD/並列向き。ただし決定性との両立にコスト、問5/§7） |
| TF(適合) | AI失敗ループ温存 | 定義の制約と矛盾（依存・工数） | **定義と完全整合** |

採用: **案C**。却下理由を ADR-001 に記録。

**精度に関する正直な契約（問1）**: 案Cの本質的利得は「精度」ではなく「**ブーリアン演算の無破綻性**」に尽きる。最終出力は案A同様テッセレーション近似であり、寸法公差は抽出解像度に依存する。プリミティブの解析寸法と出力メッシュの近似公差を**仕様で明確に区別**し、エージェントへの契約として明示する（隠蔽しない）。

## 3. スコープ

**含む**: SDFカーネル（解析プリミティブ・ブーリアン・smooth blend・offset/shell・変換・配列）/ 正本スクリプト形式（決定的・サンドボックス、問2）/ 適応的メッシュ抽出（manifold dual contouring・鋭利エッジ保持）/ 検証API（体積・質量・BBox・最小肉厚・オーバーハング・干渉・断面・寸法計測）/ 構造化エラー {code, cause, fix_hints[]} / 内蔵ソフトウェアラスタライザによるscreenshot / MCPサーバー内蔵（stdio・ツール≤12・read-only既定）/ CLI / 出力: STL・3MF・GLB・自己完結HTMLビューア / マルチインスタンス発見
**含まない（→BACKLOG.md）**: GUIアプリ、B-rep/NURBS、外部API、クラウド、CAM/CAE、アセンブリ拘束、**facetted STEP出力（問7で降格。3MF/STLと受け手が重複し決定打にならず。真B-rep STEPの需要が確認できた時点で再付議）**

## 4. フェーズ + DoD

### Phase 0 — 完成形逆算（C11）
SPEC（REQ→SPEC→TC表含む）/ 完成稿README / FAQ / MCPツールリファレンス / **評価セット定義（問7: 代表タスクN≥10件＋各合否定義＋「無人完走」の操作的定義。これがKPI分母）** / ADR-001（カーネル選定）/ ADR-002（GUI非搭載）/ **ADR-003（実装言語・ランタイム。問3: 単一バイナリ・SIMD・決定性・サンドボックスを同時に満たす言語を確定。未決のままPhase 1へ進まない）**。
**DoD**: 全文書完成・評価セット凍結・ADR-001/002/003 確定・「この機能はスコープ内か」に即答可能

### Phase 0.5 — 垂直スパイク（中核仮説の早期検証・S/M、問6で新設）
最短経路で end-to-end を1本貫通: box 1個 → 1ブーリアン → manifold DC抽出 → 1 screenshot → 最小MCP（≤3ツール）→ **Claude実機で1往復の自己修正ループ**。完成度ではなく中核仮説（AIがこのループを回せるか）のYes/No確認が目的。
**DoD**: Claude実機でスパイクが1往復成立 / 不成立なら案C・MCP設計を即見直し（後続フェーズ着手前のゲート）

### Phase 1 — SDFコア + メッシュ抽出（L）
プリミティブ（box/sphere/cylinder/cone/torus/capsule/歯車/ねじ山等）・演算・変換・決定性保証。適応的 manifold dual contouring 抽出。
**DoD**: 全演算の性質テスト（水密100%・非多様体0・体積誤差閾値内）/ 決定性は§7の範囲でバイト同一

### Phase 2 — 正本スクリプト + CLI（M）
最小DSL（AST検査・import不可・実行時間/メモリ上限・正本＝スクリプト、問2）。`kado run/render/check/export`。
**DoD**: 例10種が headless CI で通過

### Phase 3 — 検証・自己修正支援（M）
計測/検査群 + DFM lint + 構造化エラー。
**DoD**: 検証TC≥14 / 欠陥例10種全検出 / fix_hints起点の自己修正E2E 3本

### Phase 4 — MCPサーバー + screenshot（L）
`create_feature / edit_feature / delete_feature / get_scene(要約・差分) / run_script / screenshot(視点指定) / measure / validate / export / list_instances`（feature系は正本スクリプトへのシンタックスシュガー、問2）。内蔵ラスタライザでPNG（6視点+等角）。
**DoD**: 評価セット（Phase 0定義）に対し無人完走率を計測 / 「M3穴付きブラケット」を自然言語→検証済みSTLまで無人完走 / screenshot≤2秒

### Phase 5 — 出力拡張（M）
3MF（単位・メタデータ）/ GLB / 自己完結HTMLビューア出力 / UUIDソケット並列。**（facetted STEPは§3でBACKLOG降格）**
**DoD**: PrusaSlicer等の外部実ツールで開封検証 / 2インスタンスA/B並列デモ

### Phase 6 — SHIP（S）
出荷チェックリスト全PASS・gitleaks・SBOM・CrossReview・v0.1.0 zip。
**DoD**: <promise>COMPLETE</promise>。**リリース実行はrelease-approvalスキル経由・人間承認必須（G8）**

## 5. 完成形ファイル一覧

```
kado/
  src/core/        # sdf, ops, transform, primitives
  src/extract/     # 適応的メッシュ抽出（manifold dual contouring）
  src/script/      # DSL・サンドボックス・正本管理・構造化エラー
  src/verify/      # 計測・検査・DFM lint
  src/render/      # ソフトウェアラスタライザ
  src/mcp/         # server, tools, discovery
  src/io/          # stl, 3mf, glb, html_viewer
  src/cli/
  docs/SPEC.md  docs/MCP-TOOLS.md  docs/EVAL-SET.md  docs/socratic-review.md
  docs/adr/ADR-001 ADR-002 ADR-003
  README.md FAQ.md CHANGELOG.md BACKLOG.md CONTRIBUTING.md SECURITY.md LICENSE
  examples/（ブラケット/歯車/筐体/エンクロージャ等10種）
  tests/（目標300+・カバレッジ≥80%・性質テスト中心）
  .github/workflows/ci.yml（Lint→Test→Coverage→audit→SBOM）
```

## 6. リスク

| リスク | 対策 |
|--------|------|
| DSL実行 = コード実行経路（E） | ホワイトリストAST・import禁止・リソース上限。生スクリプトはopt-in（誰がopt-inするか＝人間操作者に限定し、自律エージェント既定では不可）。**リソース上限を実装で強制（問16）: JSONパーサ深さ≤128／DSL深さ≤64・ノード≤50k・ソース≤1MiB。病的入力はクラッシュせずエラー** |
| MCP書き込み（T） | プロジェクトdir限定・パストラバーサル検査・read-only既定（C9）。**書込サンドボックスを実装で強制（問15）: 絶対パス・`..`・ルート脱出を拒否しCWD配下の相対パスのみ許可** |
| 鋭利エッジの抽出品質 | 特徴保持 manifold dual contouring + 解析的法線。性質テストで角度誤差を定量ゲート |
| **メッシュ抽出の健全性（問1/問11）** | min/maxの無破綻は場の代数の話。出力メッシュの非多様体・薄肉破綻は別問題。manifold DCを必須とし、非多様体0・水密100%を性質テストでゲート |
| 寸法精度への疑念 | プリミティブ寸法は解析的厳密、**出力メッシュは公差付き近似と明示**。抽出解像度をユーザー制御可能に（問1） |
| **決定性 × SIMD/並列 の両立コスト（問5）** | 固定縮約順序・fast-math禁止・決定的並列リダクション・固定/自前超越関数。決定性の範囲は§7で限定明示 |
| スコープ膨張（v2の轍） | MCPツール上限12・本Plan外は即BACKLOG（C3）。facetted STEP降格もこの方針の一環（問7） |
| 新規開発の工数過小評価 | XL扱い。フェーズ毎に再見積もり、超過2回でフェーズ分割を付議。「依存ゼロ」を「静的リンク許容の自己完結バイナリ」に再定義し工数圧縮（問4） |

## 7. KPI と決定性の範囲

**KPI**: 無人完走率（Phase 0評価セットを分母とする）≥80% / 平均ツール呼出≤15/タスク / screenshot≤2秒 / 単一バイナリ起動≤100ms / テスト300+ / 静的解析警告0 / PII収集ゼロ（設計上）

**決定性の範囲（問5で限定）**: 「同一リリースバイナリ・同一OS/arch」内で**バイト同一**を保証する。クロスプラットフォーム間は**数値的に同等だがビット同一は非保証**（libm差・FP縮約順序差のため）。CIは各対象プラットフォームで自己再現性（同一環境2回実行のバイト一致）を検証する。

---
付議事項（承認時に併せて決定）:
① 案C採用可否
② プロダクト名: Kado（角）確定
③ Forge3D v3.0の扱い（アーカイブ/並存）
④ **正本=スクリプトの確定（問2）**
⑤ **ADR-003 実装言語の確定（問3）**
⑥ **「依存ゼロ」→「静的リンク許容の自己完結バイナリ」への制約再定義（問4）**
⑦ **決定性の範囲限定（同一バイナリ・同一arch内バイト同一、問5）**
⑧ **Phase 0.5 垂直スパイク新設の可否（問6）**
⑨ **facetted STEP のBACKLOG降格（問7）**

承認後、Phase 0から着手。
