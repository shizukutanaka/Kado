# セキュリティポリシー

Kado は AI エージェントが呼び出すローカル幾何エンジンであり、**信頼できない入力**
(AI が生成するスクリプト・MCP リクエスト) を扱うことを前提に設計されています。
セキュリティは後付けではなく中核要件です。

## セキュリティモデル（保証する不変条件）

以下はテストで固定された契約です（詳細は [`docs/SPEC.md`](docs/SPEC.md) §7、
吟味の経緯は [`docs/socratic-review.md`](docs/socratic-review.md)）。

### 1. 外部送信ゼロ

2つの独立した主張があり、**それぞれ別の手段で強制**されます（問316）。片方がもう
片方を担保しない点に注意してください:

| 主張 | 強制手段 |
|------|----------|
| ネットワークソケットを一切開かない。MCP は stdin/stdout のみ。 | `no_source_file_references_network_apis`（`tests/no_network_egress.rs`）が `src/` を再帰走査し、`std::net`・`TcpStream`・`TcpListener`・`UdpSocket`・`SocketAddr` 等の参照を禁止。 |
| 外部 crate ゼロ（std のみ・ADR-003）。サプライチェーン経由の流出経路を持たない。 | CI の `no-external-deps` ジョブ（および `scripts/check.sh`）が `cargo tree` で依存ツリーの空を強制。 |

**`cargo tree` はソケットを検出できません。** ネットワーク型は標準ライブラリの一部で
あり依存ツリーに現れないため、「外部 crate ゼロ」から「送信ゼロ」は導けません。
問316 以前はこの区別が曖昧で、実際にソケットを使っていなかったのは**契約ではなく
偶然**でした。上表の1行目がその穴を塞いでいます（ガードが実際に違反を検出することは
`src/` へ一時的に `std::net` 参照を仕込んで実地確認済み）。

### 2. 書き込みサンドボックス（パストラバーサル防御）
`export` などのファイル書き込みは **プロジェクト直下のみ** に制限されます
(`sandbox_write_path`):
- 絶対パス、`..` トラバーサル、ルート/プレフィックス付きパスを拒否。
- 空・空白のみのパスを拒否。
- Unix ではバックスラッシュは literal ファイル名として扱われ脱出不能。

### 3. DSL サンドボックス（コード実行の封じ込め）
- ホワイトリスト AST：既知の演算子のみ評価。未知演算子は拒否。
- 引数アリティ厳密：不足・過剰（0 引数含む）はエラー。
- `import` や任意コード実行の経路を持たない。

### 4. リソース上限（DoS 防御）
病的入力によるメモリ枯渇・スタックオーバーフロー・無限ループを構造的に不能化:

| 上限 | 値 |
|------|-----|
| スクリプトソースサイズ | 1 MiB |
| ノード数（共有予算） | 50,000 |
| シーン木の深さ | 64（**ワーカースレッド上でも成立**・問313 で実測。下記参照） |
| JSON ネスト深さ | 128 |
| repeat カウント | 256 |
| MCP メッセージサイズ | 16 MiB（確保前に検査） |
| 抽出解像度 | 256（この上限でのピーク RSS ≈281MB・問308 で実測。以前は ≈670MB） |
| 画像寸法 | 4096 |
| STL インポート三角形数 | 5,000,000（≈250 MB。確保前に検査・問296） |
| `measure` 光線の反復数 | 10,000 ステップ（grazing 光線の打ち切り・問299） |
| `measure` 交差数 | 64/光線（問299） |

`Sdf::eval` は木を**再帰的に**降るため、深さ上限はスタック消費と直結します。
問312 で抽出を並列化し評価が**ワーカースレッド**（Rust 既定 2MB スタック。主スレッドの
8MB より小さい）で走るようになったため、上限が依然成立するかを問313 で実測しました:

| プロファイル | 深さ64 に必要なスタック | 1フレームあたり | 2MB 既定に対する余裕 |
|---|---|---|---|
| debug | >128KB, ≤256KB | ≈2.0KB | **約8倍** |
| release | ≤32KB | ≈0.5KB | **約64倍** |

余裕が十分なため**明示的な `stack_size` 指定は行っていません**（根拠のない定数を
置かない方針）。上限の4倍（256段）まで抽出できることも回帰テストで固定しています
（`deeply_nested_tree_survives_extraction_on_worker_threads`）。
なお Rust のスタックオーバーフローは**捕捉できずプロセスが abort** するため、
この余裕の確認は「落ちないこと」を担保する唯一の手段です。

巨大 `Content-Length` は **メモリ確保前** に拒否されます（OOM 防御）。
`validate-stl`（CLI 専用）による binary STL 読み込みも、宣言三角形数を確保前に
上限照合し、ファイルサイズを読み込み前にメタデータで照合します（問296）。
インポートしたメッシュは **検証専用**で、SDF シーン正本にはしません（ADR-001）。
MCP 経由のファイル読み込みは提供しません（読み取り攻撃面を増やさない）。

### 5. 数値の安全な縮退
- 非有限パラメータ（`1e400` → ∞）はパーサ層で拒否。
- 内部計算が NaN/Inf を生んでも、JSON 出力は `null` に、座標出力は `0.0` に
  サニタイズされ、不正な出力でクライアントを壊さない。

## 想定する脅威と非脅威

- **想定する脅威**: 信頼できない AI 生成スクリプト/MCP リクエストによる、
  ファイルシステム脱出・リソース枯渇・不正出力・クラッシュ。
- **非脅威**: Kado を起動するローカルユーザは信頼されるものとする。Kado は
  サンドボックス VM ではなく、悪意あるネイティブコードの実行から OS を守る
  ものではない（外部依存ゼロ・std のみ・メモリ安全な Rust により攻撃面を最小化）。

## 脆弱性の報告

セキュリティ上の問題を発見した場合は、**公開 issue を作成せず**、リポジトリ管理者へ
非公開で報告してください（GitHub の「Report a vulnerability」機能、または
リポジトリ所有者への直接連絡）。以下を含めていただけると対応が迅速になります:

- 影響を受けるコンポーネント（CLI / MCP / 特定モジュール）
- 再現手順（トリガとなるスクリプト・リクエスト・入力）
- 想定される影響（脱出・DoS・不正出力など）

## 対応方針

- 上記の不変条件を破る報告は **回帰テストを追加** した上で修正します
  （本プロジェクトの規律：バグ修正は必ずテストで固定する）。
- 修正は決定性・外部依存ゼロの制約を維持したまま行います。
