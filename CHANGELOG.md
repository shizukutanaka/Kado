# Changelog

このプロジェクトの注目すべき変更をすべて記録する。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [Unreleased]

### 追加

- **「外部送信ゼロ」を契約化**した (問316)。`SECURITY.md` §1 の最重要主張
  「ネットワークソケットを一切開かない」だけ**強制手段が無かった**。`no-external-deps`
  ジョブ（`cargo tree`）が守るのは「外部 crate ゼロ」であり、ネットワーク型は
  **標準ライブラリの一部**なので依存ツリーに現れない——`cargo tree` は原理的に
  ソケットを検出できず、C2 から C1 は導けない。実際にソケットを使っていなかったのは
  契約ではなく**偶然**で、`std::net::TcpStream` を1行足しても既存ゲートは1つも
  落ちなかった。`tests/no_network_egress.rs` を追加し、`src/` を実行時に再帰走査して
  `std::net`・`TcpStream`・`TcpListener`・`UdpSocket`・`SocketAddr` 等の参照を禁止。
  `include_str!` ではなく `read_dir` を使うのは**まだ存在しないファイル**にも効かせる
  ため。禁止トークンは実行時の文字列連結で組み立て、テスト自身が自分に反応しない
  ようにした（除外リスト不要）。走査が0件で無言合格しないようファイル数と `lib.rs` の
  存在も固定。`src/lib.rs` へ実際にソケット型の参照を仕込んで **FAIL することを実地確認**
  済み。さらに「テスト自身が自分に反応しない」という主張も検算したところ**実際には
  破れており**（例示行のリテラル残存と、検出語の複数形が単数形へ部分一致していた）、
  `the_guard_itself_contains_no_forbidden_token` を追加してコメントから契約へ格上げした。
  `SECURITY.md` §1 は主張と強制手段が1対1で対応する表へ書き直した

- README に **MCP クライアントへの接続手順**を追加 (問315)。Kado は自らを AI-First と
  位置づけ主インターフェースを MCP としながら、**接続手順がリポジトリのどこにも
  無かった**（`mcpServers` / `claude mcp add` の記述が 0 件）。クイックスタートは CLI のみで、
  想定利用者である AI エージェントに繋ぐ最後の一歩が欠けていた。Claude Code の
  `claude mcp add` と設定ファイル方式の JSON、絶対パス必須の注意、`help` による導通確認、
  繋がらない場合の切り分け（素で起動し EOF 終了を見る）、`export` の相対パスが
  **クライアントの作業ディレクトリ**基準になる落とし穴を明記。手順は実バイナリへ
  クライアントと同じ順序（initialize→tools/list→help→run_script→screenshot）を流して
  実際に通ることを確認済み

- **品質ゲートをローカルで自動化**した (問314)。問309 以来「第5段階（自動化）は
  権限外」と結論していたが、これは要件（**ゲートが自動で走ること**）を実装手段
  （GitHub Actions）と取り違えていた。実際には CI が無い状態で**壊れたコードの push を
  止めるものが規律しか無かった**。`scripts/check.sh`（5ゲート + 依存ゼロ検査を1コマンド・
  全ステップ実行して結果を一覧・`--fast` あり）と `.githooks/pre-push`（push 時に自動実行し
  失敗なら push を中止）を追加。`.git/hooks/` はコミットできないため、**コミット済みの
  `.githooks/`** を `core.hooksPath` で指す方式にした（有効化は
  `git config core.hooksPath .githooks` の一度きり）。整形違反を注入して**実際に FAIL・
  終了コード1 になること**、`--dry-run` push でフックが発火することまで確認。
  GitHub Actions（多 OS 検証）は引き続き所有者の `git mv` 待ち

### 変更

- 並列化後も**深いシーン木が安全**であることを実測・契約化した (問313)。問312 で
  `Sdf::eval`（再帰）の実行場所が主スレッド（8MB）から**ワーカースレッド（Rust 既定
  2MB）**へ移ったが、`SECURITY.md` の「深さ64」保証は主スレッドで測られたもので、
  **そもそも深さ64 の木を抽出まで通すテストが存在しなかった**（stress_probe の最深は
  union 5段、script 側の深さテストはパーサ層の拒否のみ）。実測の結果、深さ64 に必要な
  スタックは debug で >128KB・≤256KB（≈2.0KB/段）、release で ≤32KB（≈0.5KB/段）——
  **2MB 既定に対し debug でも約8倍の余裕**。当初の見積り（数十バイト/段）より桁が
  違っており、実測しなければ気づけなかった。余裕が十分なため `stack_size` の明示は
  行わず（根拠のない定数を置かない）、深さ64 と**その4倍の256段**で threads=1/4 の
  digest がビット同一であることを回帰テストで固定。SECURITY.md に実測値を明記
- `polygonize` を **std のみで決定的に並列化**した (問312)。格子点評価は完全に独立な
  純関数評価だが単一スレッドで逐次実行していた。`std::thread::scope` で
  Phase A（格子値サンプリング）と Phase B（セル emit）を分割実行する
  （外部 crate 不要・ADR-003 維持）。**実測 2.51倍**（4コア・res=160 で 0.50s→0.20s）。
  `validate(res=256)` 全体では 3.0s→2.4s（1.25倍）にとどまる——DFM 検証側が逐次の
  ままで、Amdahl の法則どおり並列化した部分しか速くならないため。ピーク RSS は
  281→314MB（+33MB、ワーカーのローカル soup 分）。
  **決定性（問5）は「スレッド数にも非依存」へ強化**: 出力はスレッド数に関わらず
  ビット同一で、digest 3解像度が並列化前の基準値と完全一致することを実測確認。
  テスト `parallel_extraction_is_bit_identical_regardless_of_thread_count`
  （threads=1/2/4/7/64）と `split_ranges_partitions_exactly_and_deterministically` で固定


## [0.2.0] - 2026-08-18

AI エージェント向けの**計測と検証**を主題にしたリリース。寸法測定ツールの新設、
DFM 検査の偽陰性修正、恣意的閾値の解消、そして製品が自らの合格条件を検証できる
状態への到達（問276–311）。

### 追加

- **看板 KPI「無人完走率」を評価セット全体で実測**した (問311)。Plan.md は
  「無人完走率（**Phase 0評価セットを分母とする**）≥80%」と定めるが、問309 で
  実行可能にしたのは旗艦 DoD 1件のみで **1/N しか測れていなかった**。評価セットの
  全17課題を実 `kado mcp` バイナリへ流し、AI と同じ道具列（run_script→validate→
  export の**3呼出**）で完走できるかを数える統合テストを新設。**実測 17/17 = 100%**
  （既存の `eval_set_*` は Rust API を直接叩き「幾何の正しさ」を見るのに対し、本テストは
  「エージェントが MCP 越しに完了できるか」という別の問いに答える）。あわせて
  `tests/common/mod.rs` を新設して MCP ハーネスの重複を解消した
- Plan.md の**旗艦 DoD を実行可能テストにした** (問309)。「M3穴付きブラケットを
  自然言語→検証済み STL まで無人完走」と KPI「平均ツール呼出 ≤15/タスク」は
  一度も計測されていなかった。実 MCP バイナリを叩く統合テストとして実装し、
  `run_script`→`measure`→`validate`→`export` の **4呼出**で完走すること・全呼出が
  `isError:false`（＝人手の介入不要）・穴径が Ø3.2 であること・書き出した STL を
  読み直して水密であることまで検証する。あわせて全 KPI を実測し SPEC に記載
  （起動 4ms / screenshot 84ms / テスト457 / 警告0、いずれも大幅達成）
- **`measure` ツール（第一原理から導出した不足の解消）**: 光線を撃って表面交差と
  隣接交差間の距離（span）を返す。**span がそのまま穴径・肉厚・面間距離**になり、
  従来は約30回の `eval` 手組み二分探索を要した寸法確認が **1呼出**で済む。
  Plan.md の KPI「平均ツール呼出 ≤15/タスク」と旗艦 DoD「M3穴付きブラケット」を
  両立させるために不可欠だった。sphere tracing（Hart 1996）を採用し、`|d|` が真の
  距離の下界であるという Kado の SDF の性質を利用して**薄い特徴も飛び越さない**。
  距離は SDF の**符号**のみに依拠するため厳密（`eval` の大きさは合成形状で下界に
  過ぎない）。実測: M3クリアランス穴が Ø3.2 と一致 (問299)
- OVERHANG 警告に**落差（支持高さ）の実測値**を追加 (問302)。スライサ文献はオーバー
  ハングを「下方から支持されていない部分」と定義し、サポート生成は必要なサポート量の
  最小化を目標とする。つまり「何 mm 浮いているか」は角度・面積に並ぶ一次情報だが、AI は
  これを知る手段がなかった。最悪点から真下へ光線を撃ち、直下の材料まで（無ければ
  ビルドプレートまで）の距離を測って報告する。0.5mm 浮きと 20mm 浮きでは必要なサポートの
  規模もリスクも桁違いで、AI が対応を比例させられる。閾値ではなく**測定値**として報告
  （問247 の measured_min_wall と同じ思想）
- MCP `help`（AI が読む唯一のリファレンス）に **chamfer の worked example** を追加
  （平45°面取り vs 丸フィレットの対比・実用ケース）。あわせて help の実例が実際に
  eval できることを固定するメタテストを新設し、例の腐敗（op 名/引数順変更での無効化）を
  防ぐ (問298)
- **STL インポート（検証専用）**: `validate-stl <file.stl>` CLI コマンドで外部
  binary STL を読み込み DFM 検証する（`io::stl::decode_binary`）。長く保留されていた
  唯一の backlog を、ADR-001「SDF が唯一の正本」を保存する保守的設計で実装——
  インポートしたメッシュは検証・可視化専用で **SDF シーン正本にはしない**、
  mesh→SDF 再構成もしない、MCP には出さず CLI に局所化。厳格検証（長さ不一致・
  ASCII・非有限・上限超過を確保前拒否）で不正 STL をサイレント故障させない (問296)
- AI エージェント向け作業指示書を新設: `CLAUDE.md`（共通ガードレール・表面積
  チェックリスト・完了の定義・禁止事項）・`docs/agents/opus-instructions.md`
  （設計判断を伴うタスク）・`docs/agents/sonnet-instructions.md`（仕様が明確な
  タスクの手順書 + エスカレーション基準）。セッションを跨ぐ規律喪失を防ぐ (問295)
- 非一様スケール `scale_xyz(sx,sy,sz)`: 符号厳密・保守的過小評価・
  Lipschitz=1 を証明済みの安全な近似 (問276)
- PNG screenshot 応答の実圧縮: 決定的 DEFLATE (RFC 1951, 固定 Huffman・
  候補距離 {1,3} 限定 RLE) を std-only で新規実装。無圧縮 stored との
  小さい方を採用するため退行の余地なし。実測で raw 比 8.3% まで縮小 (問281)
- 自己完結 HTML ビューアにタッチ操作 (単指ドラッグ=オービット・二指ピンチ
  =ズーム) とダブルクリックでの視点リセットを追加 (問284)
- 面取りブーリアン `chamfer_union`/`chamfer_intersection`/`chamfer_difference`:
  `smooth_*` の丸いフィレットに対する**平らな (45°) 面取り**。IQ/hg_sdf の
  閉形式で決定性・std-only を維持。印刷底面の面取り・バリ取り・はめ合い
  クリアランス向け。表面厳密・内部近似の契約を SPEC に明記 (問285)
- MCP 最新安定版 **2025-11-25** への対応: `protocolVersion` 版交渉リストの
  先頭に追加し既定に採用 (2025-06-18 / 2024-11-05 も後方互換で維持)。同版で
  Implementation に追加された任意フィールド `serverInfo.description` を宣言。
  同版が明確化した「入力検証エラーは Tool Execution Error (`isError:true`) で
  返す」規約への適合 (問106 以来) をテストで固定 (問286)
- PNG screenshot に**行フィルタ** (None/Sub/Up) を追加: 行ごとに最小絶対値和
  (PNG 仕様推奨の MSAD) で決定的に選択し、平坦背景・陰影面を差分化して問281 の
  RLE がより縮むようにした。全 None 版とも比較し小さい方を採るため旧実装より
  悪化しない。実スクリーンショット (512×512, iso) で 65347→60891 バイト
  (6.8% 減) を確認。Python zlib による独立デコード・全チャンク CRC 照合で
  可逆性を検証 (問287)
- screenshot の座標軸グノモンに **mm 目盛り** を追加: 軸長から 1/10/100mm を
  決定的に選び各軸に垂直な短い目盛りを刻む。応答に目盛り間隔を記した text を
  添え、VLM が画像から寸法を概算できるようにした (文字描画はせず std-only 維持)。
  `axes=false` のときは従来通り image のみ (問288)
- 出力フォーマットの仕様適合強化: binary STL の 50 バイト三角形レコード
  (法線+3頂点+属性) のバイト配置と頂点往復を独立検証するテストを追加。
  さらに STL/3MF/GLB を **Kado 以外の標準ツール** (Python `struct`/`zipfile`/
  `xml.etree`) で開けることを確認 (3MF は `zipfile.testzip()` が全 CRC を独自
  再計算)。この外部相互運用検証手順は `docs/SPEC.md` §10 に記載 (問289;
  当初 `docs/ci-recommendation.md` を作ったが既存 `docs/ci.yml` と重複のため
  問295 で削除・集約)
- GLB 出力に **頂点法線 (NORMAL)** を追加: 面積重み付きの平滑法線を決定的に
  計算し `POSITION`/`NORMAL`/`indices` の 3 アクセサ構成で書き出す。従来は
  POSITION と索引のみで、ブラウザ・Windows 3D ビューア・Blender 等が
  平坦/既定シェーディングになっていたのを、正しい滑らかな陰影で表示できる
  ようにした (std-only・決定性維持)。独立 glTF パーサで全法線が単位長で
  あることを検証 (問290)
- GLB 出力に **既定 PBR 材質** を追加: metallic=0・roughness=0.6・中立グレーの
  マット誘電体を明示。法線追加後もビューアが glTF 既定材質 (metallic=1 = 暗い
  ラフ金属) にフォールバックして見栄えが悪かったのを、素直なマットプレビューに
  した。表示上の既定値でありビューア側で上書き可能 (問291)

### 修正

- README の**ドリフトを修正し、恒久ガードを新設** (問310)。MCP ツール表から
  `measure`（問299）が、CLI 表から `validate-stl`（問296）が漏れており、README が
  実装より古い状態だった。問278 は README の**演算子**一覧を守るメタテストを入れて
  いたが、**ツール表**と **CLI コマンド表**は無防備で、同じ「表面積の不整合」
  （問292/294）がドキュメント側で再発していた。両表を実装と同期し、
  `readme_lists_every_mcp_tool` / `readme_lists_every_cli_command` を新設して
  `tool_list` を真実源に README を検証する（README から `measure` を消すと実際に
  落ちることを確認済み）
- 抽出時の**メモリを 58% 削減**した (問308)。`polygonize` は格子点の座標を
  `poss: Vec<Vec3>` に全点保存していたが、座標は添字から一意に決まるため純粋な冗長
  だった。res=256 (n=257) では 257³×24B ≈ 407MB を値配列に上乗せしており、実測ピーク
  RSS は 670MB。都度再計算に変えて **670MB→281MB**、実行時間も **5.2s→3.0s**
  (キャッシュ効率の改善) となった。式・演算順序は保存版と同一のため出力は
  **ビット単位で不変**（digest 3解像度で一致を実測確認）。関連ソフトウェア
  (libfive 等の SDF CAD) が全格子を実体化せず階層評価する設計との比較から着眼した
- レンダラの**陰影がガンマ空間で計算されている**ことを実測・文書化し、現行挙動を
  テストで契約として固定した (問307)。`diffuse`(sRGB 値) に輝度係数を直接乗じており、
  リニア空間で行うべき光の演算としては物理的に誤り。実測差は diffuse=220・
  intensity 0.25 で 55 対 117（全域の24%）と大きく、陰の面の階調が失われている。
  ただし修正は全 screenshot の出力を変える一方 VLM の形状認識向上は未計測なので、
  **出力は変えず**乖離の記録と契約化に留めた（判断は利用者に委ねる）
- `volume` / `surface_area` の**離散化誤差を実測し、契約として固定**した (問306)。
  `help` は AI に「mass_g = volume/1000 × density」で材料費を見積もれと指示し、
  `surface_area` も「造形時間・材料費の主要因」と説明しているが、これらは**抽出メッシュ
  由来の近似**であり、精度はどこにも文書化されず**テストでも固定されていなかった**——
  正確だったのは偶然であり契約ではなく、抽出器を変えれば無音で劣化しうる状態だった。
  既定解像度48での実測: 体積は -0.05〜-0.12%（材料費見積もりに十分）、表面積は
  -0.06〜-1.02% と**収束が遅く**箱状形状では解像度128でも -0.44% で頭打ち。
  いずれも内接多面体近似のため**常に過小評価**（安全側）。回帰テストで劣化検知を固定し、
  validate スキーマ・help・SPEC §9 に実測値を明記して AI が解像度を選べるようにした
- HIGH_ASPECT_RATIO の**ハードコード閾値を解消** (問305)。閾値 8.0 が関数内 const で
  固定され、コメントは「業界ガイドラインの目安」と主張していたが、FDM 実務ガイドの
  実測目安は 0.3mm 幅→約7mm 高さ (比 ~23)、1.5mm 幅→約30mm (比 ~20) で、**安全な比は
  絶対寸法・ノズル径・材料に依存し普遍値は存在しない**。CLAUDE.md §1「恣意的閾値を
  持ち込まない」に反し、`min_wall_mm`/`max_overhang_deg` がユーザーパラメータである
  設計とも非対称だった。`max_aspect_ratio` 引数を追加 (既定 8.0 で挙動互換・0 でスキップ)。
  あわせて**実測比を `aspect_ratio` として Report/JSON へ常時公開** — 従来は閾値超過時の
  警告文にしか現れず、合格時のマージンが読めなかった (問247 `measured_min_wall` と同じ
  思想)。警告 hints には文献の実測目安を載せ、AI が寸法に応じて閾値を選べるようにした。
  既存の `validate`/`validate_with_field` は新 `validate_full` へ委譲する薄いラッパと
  したため、既存 75 呼び出しは無変更で動く
- UNSTABLE の**偽陰性を修正** (問304)。静力学の標準条件は「重心の投影が**接地点の
  凸包 (support polygon)** の内側にあること」だが、旧実装は軸整列 bbox で判定していた。
  bbox は凸包の**外接近似**なので「bbox の内側だが凸包の外側」——三脚・L 字ベースの
  欠けた隅——に重心がある形状は、実際には転倒するのに**警告されなかった**
  (旧コメント自身も「内側は安定を保証しない」と限界を明記していた)。接地点の凸包を
  Andrew's monotone chain で求めて判定するようにした。実測: 正方形の3隅にだけ脚を置き
  欠けた隅へ質量を寄せた部品は、旧実装で issue ゼロ・新実装で UNSTABLE を報告。
  凸包が退化する場合 (点・線接地) は判定不能として従来の bbox 判定へフォールバックする
  (偽陽性を増やさない)。決定性は f64 全順序ソートで担保
- OVERHANG の**偽陰性を修正** (問303)。支持判定の標本点が「重心の真下・**ベッド直上**」
  だったため、「ベッド高さに材料がある」ことが支持の代理になっており、**空隙を挟んだ
  天井**が見逃されていた——ブロックを貫く 16mm スロットの天井が一切報告されなかった
  (問302 で実測確認した偽陰性)。標本点を「**面のすぐ下**」へ移し、スライサ文献の定義
  「下方から支持されていない部分」に対応させた。標本数は1点のままで計算量は不変。
  平坦底面 dome の rim は従来どおり支持済みと判定される (偽陽性を作っていない)。
  問302 の落差実測と組み合わさり、2mm スロット (短いブリッジ=印刷可) と 16mm スロット
  (要対策) を AI が区別できるようになった
- `measure` の**打ち切りがサイレントだった**のを修正 (問301)。反復上限/交差数上限に
  達しても結果に何の印も付かず、AI は「交差なし（＝何も無い）」と「途中で諦めた
  （＝まだ先にあるかも）」を区別できなかった。特に**面に沿って滑る光線**は `|d|=0` で
  歩幅が 1e-6mm に張り付き、10,000 歩でも 0.01mm しか進めず黙って空を返していた
  （この収束の遅さは Keinert et al. 2014 が扱った既知の性質）。応答に `complete` と、
  不完全時の原因・対処を述べる `warning` を追加し、サイレント故障を作らない規律
  (CONTRIBUTING §4) に適合させた
- DFM 肉厚探針 `min_wall_probe` を固定ステップから **sphere tracing** へ (問300)。
  旧実装は `diag/256` 刻みで行進していたため**それより薄い壁を跨いで見落とす**限界が
  あった（docstring に明記されていた）。`|d|` が真の距離の下界であることを使い
  表面を跨がない前進に変えたことで、この false negative を構造的に解消。交差点の
  厳密化も線形補間から符号の二分探索へ。実測: 0.3mm 板（旧ステップ 0.63mm）が
  `THIN_WALL` として検出されるようになった。測定の定義は従来どおり **ray 法**で、
  rolling ball 法（内接最大球）との差異は SPEC に明記
- AI 契約のバグ3件を修正 (問299): (1) `validate` のスキーマが `severity:"error"|"warning"`
  と宣言しながら `ENCLOSED_CAVITY` は `"info"` を出しており **AI に嘘の列挙を渡していた**
  のを修正しメタテストで恒久ガード化、(2) 実行時注入の `resolution` フィールドを文書化、
  (3) `EMPTY_MESH` のヒントが「bounding box を広げよ」と誤誘導していた（sampling box は
  SDF から自動導出され AI に操作できない）のを、実際に直せる4原因を指す内容へ差し替え
- JSON パーサ (untrusted な MCP 入力経路) の不正**数値**入力に対する非パニック性を
  回帰テストで固定 (`malformed_numbers_are_rejected_not_panicked`)。`-`・`1e`・
  `1..2` 等がパニックせず Err を返すことを観測可能にした。バッキングの Rust
  `f64::from_str` が `"1."`/`"01"` を許容する寛容性も記録 (問297)
- 開発プロセスの規律違反2件を自己監査で発見し訂正 (問295): (1) 問289 で作った
  `docs/ci-recommendation.md` が既存の `docs/ci.yml` (v102 から存在・multi-OS +
  外部依存ゼロ検証を持つ上位互換) と重複していたため削除し、固有価値 (外部相互
  運用検証手順) を `docs/SPEC.md` §10 へ集約。(2) `CONTRIBUTING.md` は「手整形の
  ため fmt を強制しない」としていたが v151 で全ツリーが rustfmt 標準へ正規化
  されていた実態に合わせ、fmt --check を品質ゲート化 (`docs/ci.yml`・CONTRIBUTING・
  SPEC §10 を更新)
- 面取りブーリアン `chamfer_*` (問285) が **JSON 形式では動くのにテキスト DSL
  では "unknown function" になっていた**のを修正。JSON 評価器と `ALL_DSL_OPS`
  には追加したが、テキスト DSL の関数ディスパッチ (`dsl.rs`) への追加を
  忘れていた。あわせて `ALL_DSL_OPS` の全 op がテキスト DSL でディスパッチ
  されることを保証するメタテストを新設し、同種の取りこぼしを防ぐ (問292)
- MCP エンドツーエンド統合テスト (`tests/mcp_workflow.rs`) を新設: 実 `kado mcp`
  バイナリを子プロセスとして起動し、AI ワークフロー (initialize→run_script→
  validate→screenshot) を stdio JSON-RPC で通しテストする。問292 の回帰
  (chamfer のテキスト DSL) はこのテストで即座に落ちることを確認済み。
  `src/mcp/mod.rs` の古いモジュール説明 (2024-11-05・Phase 0.5・ツール数≤12) も
  実態 (2025-11-25 版交渉・8ツール) に更新 (問293)
- MCP ツール面のドリフト検知メタテストを新設: 全ツールが `tool_list` (スキーマ)・
  `call_tool` (ディスパッチ)・`tool_annotations` (安全ヒント) の3箇所で整合する
  ことを固定。宣言したのに呼べない/別物として注釈されるバグ (問292 と同型) を
  防ぐ。annotation のサイレントフォールバックを実際に検知することを確認 (問294)
- `docs/SPEC.md` の対象バージョンヘッダが 0.0.1 のまま更新漏れしていたのを
  0.1.0 へ修正 (問277)
- `README.md` の演算子一覧から `prism`・任意軸 `rotate`・`scale_xyz` が
  漏れていたのを修正し、README を含む3箇所の演算子一覧の文書完全性テストを
  新設 (問278)
- CHANGELOG.md 自身が、まだ作成されていない git tag / GitHub Release への
  リンクを先回りして記載していたのを削除 (問280)
- `Plan.md` が「存在する」前提で書いていた `ADR-001`（カーネル選定）・
  `ADR-002`（GUI非搭載）が実際には一度も作成されていなかったのを
  `docs/adr/` に追加。`src/extract/mod.rs` の「Marching Tetrahedra は
  暫定実装・dual contouring に置換予定」というコメントも、実際には
  MT のまま全リリースを通過し置換の必要性が生じていない実態に更新 (問282)
- HTML ビューアがシェーダのコンパイル/リンク失敗を確認しておらず、
  失敗時に空白キャンバスのまま無言で壊れていたのを、エラー内容を画面に
  表示するよう修正。ズーム距離が無制限だったため大きくズームアウトすると
  対象物が固定 far クリップ面を超えて消える退化バグも、near/far を
  現在距離に追従させることで修正 (問284)

## [0.1.0] - 2026-07-06

初の対外品質リリース。AI エージェントが 3D 形状を生成・検証・製造出力するための
ローカル完結幾何エンジン。

### 幾何カーネル (SDF)

- プリミティブ 9 種: `sphere` / `cuboid` / `cylinder` / `torus` / `cone` /
  `capsule` / `rounded_box` / `ellipsoid` (IQ 近似・符号厳密) /
  `prism` (正多角形・厳密 SDF)
- CSG 6 種: `union` / `intersection` / `difference` + smooth 版 3 種
- 変形 12 種: `translate` / `scale` / `offset` / `shell` / `repeat` (有限反復) /
  `mirror_x/y/z` / `rotate_x/y/z` / `rotate` (任意軸・Rodrigues) /
  `cut` (平面) / `flatten` (FDM 平坦底面)
- 決定的メッシュ抽出 (Marching Tetrahedra): 同一バイナリ・同一 arch で
  バイト同一、`digest()` (FNV-1a) で再現性を観測可能

### 製造性検証 (DFM)

- 11 種の issue コード (`EMPTY_MESH` 〜 `ENCLOSED_CAVITY`)。
  空間的な issue は全て問題箇所の 3D 座標 (`location`) 付き
- 実測値レポート: 体積 (mm³)・表面積 (mm²)・実測最小肉厚・重心・
  ボディ/空洞数・ベッド接地面積
- SDF 内向きレイ探針による局所薄肉検出、ビルド方向対応オーバーハング解析

### 入出力

- 形状スクリプト: JSON とテキスト DSL の 2 表層構文 (自動判別・意味論は単一)
- エクスポート 4 形式: binary STL / GLB (glTF 2.0) / 3MF (mm 単位宣言) /
  自己完結 HTML ビューア — すべて決定的エンコード
- PNG スクリーンショット (7 視点プリセット・SSAA・透視/正射影・座標軸表示)

### インターフェース

- CLI: `version` / `selftest` / `export` / `screenshot` / `run` / `check` /
  `mcp` / `help`
- MCP サーバ (JSON-RPC 2.0, stdio, プロトコル版交渉 2025-06-18/2024-11-05):
  `run_script` / `eval` / `validate` / `screenshot` / `export` / `get_scene` /
  `undo_script` / `help` の 8 ツール (tool annotations 付き)

### セキュリティ・堅牢性

- 外部送信ゼロ (ネットワーク I/O なし)・外部 crate ゼロ (Rust std のみ)
- 書き込みサンドボックス (プロジェクト直下のみ・パストラバーサル拒否)
- DoS 上限: ソース 1MiB・50,000 ノード・深さ 64・メッセージ 16MiB・
  解像度 256・画像 4096px
- 不正な MCP フレーム本文 1 通でセッションが落ちない (Parse error で継続)
- テスト 371 本 (単体 + 統合 eval-set/敵対的ストレス) 全通過・
  clippy / rustfmt / rustdoc 警告ゼロ

<!-- 問280: バージョン比較/リリースページへのリンクは、実際に git tag と
     GitHub Release が作成されてから追加する。存在しないタグ/リリースへの
     リンクを先に書くと 404 になるため、ここでは意図的に省略する。 -->
